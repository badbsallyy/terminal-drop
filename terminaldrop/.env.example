# Base URL for the application (required for production)
NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app

# Vercel KV (Optional - for Vercel deployment with KV storage)
# These are automatically set by Vercel when you add a KV store
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Vercel Blob (Optional - for Vercel deployment with Blob storage)
# These are automatically set by Vercel when you add a Blob store
# BLOB_READ_WRITE_TOKEN=

# Note: Without Vercel KV and Blob, the app uses local file storage
# Local storage is suitable for development but not recommended for production
